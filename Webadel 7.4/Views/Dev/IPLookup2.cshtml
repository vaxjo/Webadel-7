@model List<string>
@{ ViewBag.Title = "IP Lookup"; }

<h2>Users from @ViewBag.IP</h2>

@foreach (var username in Model) {
    <p>@username</p>
}

@if (Model.Count == 0) {
    <div class="alert alert-info">No users from that IP address.</div>
}